
function check() {
    let valueInput = document.getElementById("name").value;
    if (valueInput.length > 5) {
        alert("skibifdi");
    }

    if (valueInput.includes(" ")){
        alert("helo");
    }
}  